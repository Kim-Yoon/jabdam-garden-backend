[pytest]
# 테스트 설정
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 출력 설정
addopts = -v --tb=short

# 비동기 모드
asyncio_mode = auto

# 경고 무시 (선택사항)
filterwarnings =
    ignore::DeprecationWarning
